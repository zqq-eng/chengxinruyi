<!-- pages/home/home.wxml -->
<particle-bg>
  <view class="home-page">

    <!-- 顶部丝带背景 -->
    <view class="top-ribbon"></view>

    <!-- 浮动装饰：云朵 + 花朵 + 闪烁星星（不挡点击） -->
    <view class="float-deco deco-cloud"></view>
    <view class="float-deco deco-flower-left"></view>
    <view class="float-deco deco-flower-right"></view>
    <view class="float-deco deco-star-1"></view>
    <view class="float-deco deco-star-2"></view>

    <!-- 顶部信息卡片 -->
    <view class="fancy-card card-hello">
      <view class="fancy-border"></view>

      <view class="home-hello-row">
        <view class="hello-left">
          <view class="hello-title">
            你好，{{userInfo.name || '同学'}}
          </view>
          <view class="hello-sub">
            现在是{{timePeriod}}，我们一起好好照顾身体和情绪吧 ✨
          </view>
        </view>
        <view class="hello-right">
          <view class="hello-orbit">
            <view class="orbit-dot"></view>
            <view class="hello-paw-icon">🐾</view>
          </view>
        </view>
      </view>

      <view class="body-info">
        <view class="info-block">
          <view class="info-label">身高</view>
          <view class="info-value">
            {{userInfo.height || '--'}}<text class="info-unit">cm</text>
          </view>
        </view>

        <view class="bmi-circle-block">
          <view class="bmi-ring">
            <view class="bmi-inner"></view>
            <view class="bmi-text">{{todayBMI || '--'}}</view>
          </view>
          <view class="bmi-label">BMI</view>

          <!-- ✅ 新增：BMI 对比（只加这里） -->
          <view class="compare" wx:if="{{bmiDiffText}}">
            <text class="trend up" wx:if="{{bmiTrend === 1}}">↑ {{bmiDiffText}}</text>
            <text class="trend down" wx:elif="{{bmiTrend === -1}}">↓ {{bmiDiffText}}</text>
            <text class="trend flat" wx:else>—</text>
          </view>
        </view>

        <view class="info-block">
          <view class="info-label">今日体重</view>
          <view class="info-value">
            {{todayWeight || '--'}}<text class="info-unit">kg</text>
          </view>

          <!-- ✅ 新增：体重对比（只加这里） -->
          <view class="compare" wx:if="{{weightDiffText}}">
            <text class="trend up" wx:if="{{weightTrend === 1}}">↑ {{weightDiffText}}</text>
            <text class="trend down" wx:elif="{{weightTrend === -1}}">↓ {{weightDiffText}}</text>
            <text class="trend flat" wx:else>—</text>
          </view>
        </view>
      </view>

      <view class="today-tips">
        {{todayTip}}
      </view>

      <view class="btn-area small-btn-area">
        <button class="small-btn" bindtap="goRecord">
          📅 日历记录
        </button>
      </view>
    </view>

    <!-- ✅ 今日运动打卡（改成大圆签到 + 次数 + 撤销一次） -->
    <view class="fancy-card checkin-card">
      <view class="fancy-border"></view>

      <view class="checkin-top">
        <view class="checkin-title">今日运动打卡</view>
        <view class="checkin-count">{{checkinToday}} / {{checkinLimit}}</view>
      </view>

      <view class="checkin-center">
        <!-- 大圆按钮：点一次 +1 -->
        <view class="checkin-circle" bindtap="tapCheckin">
          <view class="checkin-circle-text">签到</view>
        </view>

        <view class="checkin-center-sub">
          今日：{{checkinToday}} / {{checkinLimit}}
        </view>

        <view class="checkin-total">
          累计已打卡：{{checkinTotal}} 次 (可用于商城积分兑换)
        </view>

        <!-- 撤销一次：每天仅一次机会 -->
        <button class="checkin-undo-btn" bindtap="undoCheckin">
          撤销一次
        </button>

        <view class="checkin-sub">
          每天最多 {{checkinLimit}} 次，像签到一样把运动坚持下来。
        </view>
      </view>
    </view>

    <!-- ✅ 并列入口卡片（拍照饮食 / 助眠解压） -->
    <view class="proto-row">
      <view class="proto-card" bindtap="goDietPhoto">
        <view class="proto-icon">📸</view>
        <view class="proto-main">
          <view class="proto-title">拍照记录饮食</view>
        </view>
      </view>

      <!-- ✅ 修复：原来 bindtap="goSleep"（不存在），改为 goSleepMusic -->
      <view class="proto-card" bindtap="goSleepMusic">
        <view class="proto-icon">🌙</view>
        <view class="proto-main">
          <view class="proto-title">助眠解压</view>
        </view>
      </view>
    </view>

    <!-- ✅ 运动视频长卡片 -->
    <view class="fancy-card video-card">
      <view class="fancy-border"></view>

      <view class="video-top">
        <view class="video-title">运动视频</view>
        <view class="video-sub">跟练 5-10 分钟也很棒，轻松开始。</view>
      </view>

      <view class="btn-area workout-btn-area">
        <button class="workout-btn" bindtap="goWorkoutPlan">
          🎥 前往运动视频
        </button>
      </view>
    </view>

    <!-- 配合运动的小计划（原结构不动，只换色） -->
    <view class="fancy-card workout-card">
      <view class="fancy-border"></view>

      <view class="workout-title">配合运动的小计划</view>
      <view class="workout-sub">
        根据今天的状态，选择一段温柔的运动或视频训练，哪怕只是 10 分钟，也是在为自己积累力量。
      </view>

      <view class="workout-tags">
        <view class="workout-tag">拉伸舒展</view>
        <view class="workout-tag">轻量有氧</view>
        <view class="workout-tag">放松冥想</view>
      </view>

      <view class="btn-area workout-btn-area">
        <button class="workout-btn" bindtap="goWorkoutPlan">
          ✅ 前往运动打卡
        </button>
      </view>
    </view>

    <!-- 温柔建议 + 菜谱推荐 -->
    <view class="fancy-card suggestion-card">
      <view class="fancy-border"></view>

      <view class="section-title">今日温柔建议 · {{currentCare.label}}</view>
      <view class="section-subtitle">
        不同时间的小小提醒，帮你把一天过得更温柔、也更有节奏感。
      </view>

      <view class="time-list">
        <view class="time-block">
          <view class="time-badge">{{currentCare.label}}</view>

          <view class="time-lines">
            <view
              class="time-line"
              wx:for="{{currentCare.items}}"
              wx:key="idx"
            >
              ❀ {{item}}
            </view>
          </view>
        </view>
      </view>

      <view class="divider"></view>

      <view class="section-title mt16">简单菜谱推荐 · {{currentDiet.label}}</view>
      <view class="section-subtitle">
        按照早中晚的节奏搭配饮食，让胃和心情都轻松一点。
      </view>

      <view class="time-list">
        <view class="time-block recipe-block">
          <view class="time-badge badge-blue">
            {{currentDiet.label}}
          </view>
          <view class="time-lines">
            <view
              class="time-line"
              wx:for="{{currentDiet.items}}"
              wx:key="idx"
            >
              ⭐ {{item}}
            </view>
          </view>
        </view>
      </view>

    </view>

    <view class="tabbar-space"></view>
  </view>
</particle-bg>
